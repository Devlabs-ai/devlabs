{
  "id": "l1-same-job-in-spark-sql",
  "number": 12,
  "title": "Same Job in Spark SQL",
  "contentSource": "minio",
  "description": "Re-implement Aggregate Product Totals using Spark SQL only (temp view + spark.sql), not DataFrame groupBy/agg. Line revenue = quantity * unit_price * (1 - discount_pct), round half-up to 2 decimals per line, then sum. Output: product_id, total_units, total_revenue, txn_count, store_count. Write Parquet with overwrite.",
  "difficulty": "L0",
  "tags": [
    "spark",
    "l0",
    "sql",
    "aggregation",
    "dataframe",
    "parquet"
  ],
  "category": "batch-processing",
  "sandboxType": "spark-platform",
  "play": {
    "domainId": "data-engineer",
    "panelId": "spark"
  },
  "problemStatement": {
    "overview": "SQL aggregate of discounted product totals; write Parquet to OUTPUT_PATH.",
    "symptoms": [
      "Same business result as Aggregate Product Totals, SQL API required."
    ],
    "yourTaskSteps": [
      "Read INPUT_PATH and register a temp view.",
      "spark.sql aggregate with per-line discounted revenue.",
      "Write product_id, total_units, total_revenue, txn_count, store_count.",
      "Submit is graded with a row-level compare against each selected testcase expected/."
    ],
    "yourTask": "Implement src/main.py using Spark SQL for the product totals job.",
    "hints": [
      "createOrReplaceTempView(\"sales\") then spark.sql(...)",
      "ROUND(quantity * unit_price * (1 - discount_pct), 2) per line before SUM",
      "COUNT(DISTINCT store_id) for store_count",
      "Do not use DataFrame groupBy/agg for the solution path."
    ],
    "inputSchema": [
      {
        "column": "txn_id",
        "type": "INT"
      },
      {
        "column": "product_id",
        "type": "INT"
      },
      {
        "column": "store_id",
        "type": "INT"
      },
      {
        "column": "quantity",
        "type": "INT"
      },
      {
        "column": "unit_price",
        "type": "DECIMAL(12,2)"
      },
      {
        "column": "discount_pct",
        "type": "DECIMAL(5,4)"
      }
    ],
    "expectedOutput": [
      {
        "column": "product_id",
        "description": "Group key"
      },
      {
        "column": "total_units",
        "description": "sum(quantity)"
      },
      {
        "column": "total_revenue",
        "description": "sum of rounded line revenues"
      },
      {
        "column": "txn_count",
        "description": "count(*)"
      },
      {
        "column": "store_count",
        "description": "count(distinct store_id)"
      }
    ]
  },
  "platformSpec": {
    "contentSource": "minio",
    "testcasesPrefix": "s3a://devlabs-data/challenges/l1-same-job-in-spark-sql/testcases/",
    "runCases": [
      "01-smoke-basic",
      "02-smoke-edges"
    ],
    "submitCases": [
      "03-full-mix",
      "04-full-zero-discount",
      "05-full-high-discount",
      "06-full-multi-store",
      "07-full-balanced",
      "08-full-stress"
    ],
    "inputPath": "s3a://devlabs-data/challenges/l1-same-job-in-spark-sql/testcases/",
    "evalSolutionPath": "s3a://devlabs-data/challenges/l1-same-job-in-spark-sql/testcases/",
    "outputFormat": "parquet",
    "gradeKeys": [
      "product_id"
    ],
    "language": "python",
    "starterFileName": "src/main.py",
    "limits": {
      "driver": 1,
      "driverMemory": "1g",
      "executors": 2,
      "executorCores": 1,
      "executorMemory": "1g"
    },
    "gradeChecks": [
      "Spark job succeeds on selected Submit testcases",
      "Solution uses Spark SQL (temp view + spark.sql)",
      "Each Submit testcase expected/ matches candidate rows"
    ]
  },
  "dataGen": {
    "pattern": "A",
    "dir": "challenges/l1-same-job-in-spark-sql",
    "script": "generate.py",
    "runScript": "run.sh",
    "jobYaml": "job.yaml",
    "s3Prefix": "challenges/l1-same-job-in-spark-sql"
  }
}
